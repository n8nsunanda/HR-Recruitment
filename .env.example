# ---------------------------------------------------------------------------
# HR Recruitment – Environment variables (copy to .env.local for local dev)
# ---------------------------------------------------------------------------

# Admin dashboard: password-based login (store a strong password)
ADMIN_PASSWORD=your_secure_admin_password

# Vercel Blob – resume storage (auto-set when you connect a Blob store in Vercel)
# Local dev: create a Blob store in Vercel Dashboard → Storage → Connect, then run: vercel env pull
BLOB_READ_WRITE_TOKEN=

# Google Service Account (JSON string – entire key file as one line)
# Create in Google Cloud Console; only Sheets API needed. Share the Sheet with the SA email.
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@....iam.gserviceaccount.com","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# Google Sheet ID (from the sheet URL: .../d/SHEET_ID/...)
GOOGLE_SHEET_ID=your_google_sheet_id

# Google Sheet tab name (the name of the tab at the bottom, e.g. "Sheet1" or "Candidates"). Default: Candidates
GOOGLE_SHEET_TAB_NAME=Sheet1

# Optional: Google AdSense – ad slot IDs (from AdSense → Ad units → get code).
# Use one slot for all positions, or separate slots for left/right/bottom.
# NEXT_PUBLIC_ADSENSE_SLOT=1234567890
# NEXT_PUBLIC_ADSENSE_SLOT_LEFT=1234567890
# NEXT_PUBLIC_ADSENSE_SLOT_RIGHT=1234567891
# NEXT_PUBLIC_ADSENSE_SLOT_BOTTOM=1234567892

# Optional: LinkedIn – show recommendations on candidate page
# Your LinkedIn profile URL (shown as "View on LinkedIn" link)
# LINKEDIN_PROFILE_URL=https://www.linkedin.com/in/your-profile/
# Recommendations from Google Sheet tab "Recommendations" (Author, Text, Date). Or set JSON here:
# LINKEDIN_RECOMMENDATIONS_JSON=[{"author":"Name","text":"Quote...","date":""}]

# ---------------------------------------------------------------------------
# Optional: Gmail notification (not implemented in this starter)
# GMAIL_USER=
# GMAIL_APP_PASSWORD=
# ---------------------------------------------------------------------------
